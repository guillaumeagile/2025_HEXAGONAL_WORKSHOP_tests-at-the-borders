package stock.domain.events

// TODO: implement stock domain events
// Workshop exercise:
//   sealed interface EvenementDeStock {
//       data class StockApprovisionne(val produitId: String, val quantite: Quantite) : EvenementDeStock
//       data class StockVendu(val produitId: String, val quantite: Quantite) : EvenementDeStock
//       data class ProduitRetireduStock(val produitId: String) : EvenementDeStock  // reacts to catalog.ProduitDeprecied
//   }
